<?php


// echo "Hello Transform!";

//require_once '01_extract.php';

/*
$playlist_data = mapCoordinatesToCities($playlist_data);
$playlist_data = filter_Data($playlist_data);

// print_r($playlist_data);


// Funktionen
function mapCoordinatesToCities($data)
{
    // Annahme: Die neue API liefert bereits Städtenamen mit, daher wird diese Funktion nicht benötigt.
    return $data;
}

function filter_data($data)
{
    $filtered_data = [];
    foreach ($data as $item) {
        // Keep only the specified values
        $filtered_data[] = [
            'title' => $item['title'],
            'artist' => $item['artist'],
            'playFrom' => $item['playFrom'],
            'imageUrl' => $item['imageUrl'],
            'audioUrl' => $item['audioUrl']
        ];
    }
    return $filtered_data;
}
*/
